//#include "graph.h"

int main()
{
    //graph adjList;

    //importGraph(adjList);
    //printGraph(adjList);

    //cout << "\nIn degree of 3: " << inDegree(adjList, 3);
    //cout << "\nOut degree of 3: " << outDegree(adjList, 3);
    //cout << "\nAverage connections: " << averageConnections(adjList);
    //cout << "\nShortest path from 0 to 2: " << shortestPath(adjList, 0, 2).first;
    //cout << std::endl << std::endl;
    //topTen(adjList);
    return 0;
}

